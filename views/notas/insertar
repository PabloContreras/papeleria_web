<?php
require '/scripts/blog.sql'
$conn = new sqli ($servername, $username, $password, $dbname);

if($conn->connect_error){
    die ("Conexión fallida" .$conn->connect_error)
}
$insert = "INSERT INTO blog (id, turista_id, content)";
?> 